#include<iostream>
using namespace std;

void display(int array[],int key)
{
    for(int i=0;i<n;i++)
    {
    cout<<array[i]<<" ";
    }
}cout<<endl;

int search_arr(int array[],int key)
{
    for(int i=0;i<=n;i++)
    {
        for(int j=i;j<=n;j++)
        {
            
        }
    }
}

int main()
{
    int n;
    cout<<"Enter the size of array:";
    cin>>n;

    int array[n];
    for(int i=0;i<n;i++)
    {
        cin>>array[i];
    }

    search_arr(array,key);
    display(array,key);
    return 0;
}